# 待确认清单与行动清单

> 配套文档：`实现文档.md` v0.1 ｜ 本文档为执行前的最小闭环清单

---

## 第一部分 · 待确认清单

### A 类：需要**决策**（存在取舍，需拍板）

| # | 事项 | 背景 | 可选方案 | 推荐 | 不确认的影响 |
| --- | --- | --- | --- | --- | --- |
| D1 | 适用范围 | 决定合规边界 | ① 组织内部/社区议事 ② 法定选举 | **①**（②直接否决） | 【阻断】合规红线，方案无法定稿 |
| D2 | 匿名强度 | 决定是否引入 ZK | ① 全匿名（隐身份+隐选项） ② 仅隐身份 ③ 实名公开 | **①** | 【阻断】架构分叉最大的一项，合约与前端全量返工 |
| D3 | 是否允许改票 | 影响状态机与不变量 | ① 不允许 ② 允许（覆盖旧票） | **①** | 【阻断】`Election.sol` 核心逻辑无法开写 |
| D4 | 单选 / 多选、选项上限 | 影响数据结构与 gas | ① 单选、≤16 ② 多选、≤16 | **①**（预留多选位） | 阻断数据结构冻结 |
| D5 | 单场选民规模上限 | 决定去重方案 | ① <5 万 ② 10 万 ③ >50 万 | **② 10 万 → 位图** | 【阻断】去重方案选错，规模上来后必须改合约 |
| D6 | 公开链 / 联盟链 | 决定整体架构 | ① EVM 公开 L2 ② FISCO BCOS / Fabric | **①**（除非有强制要求） | 【阻断】工具链、ZK 方案、审计对象全部不同 |
| D7 | 生产主网 | 影响成本与生态 | ① Base ② Arbitrum One ③ Polygon PoS | **① Base** | 非阻断（可测试网先行），但影响部署脚本与地址表 |
| D8 | 是否引入免 gas 投票 | 决定后端职责边界 | ① 不做（用户自付） ② EIP-712 + 中继代付 | **① 一期不做，二期评估** | 非阻断；但代付需额外风控与预算 |
| D9 | 匿名凭证实现路径 | 决定开发量 | ① Semaphore v4 ② 自研 circom 电路 | **①** | 非阻断但强影响排期（自研约 +4 周且审计成本翻倍） |
| D10 | 是否叠加生物核身 / 设备指纹 | 影响女巫防护强度 | ① 不叠加 ② 叠加 | **①** | 非阻断，但决定 R2 女巫风险的残余水平 |
| D11 | 数据出境评估 | 决定 RPC / Pinning 供应商可选范围 | ① 需评估 ② 不需要 | **① 需评估** | 【阻断】选错供应商后需迁移，返工成本高 |
| D12 | 验收阈值 | 覆盖 CI 门禁与验收 | 覆盖率 90%/85%、gas 上限、QPS、首屏 | **见实现文档 6.1 / 5.3 / 4.x DoD** | 【阻断】CI 门禁无法配置，测试无法判定通过 |
| D13 | 单票成本上限 | 影响链选择与优化力度 | ① <0.005 USD ② <0.02 USD ③ 不限 | **②** | 非阻断，但决定是否需要更多 gas 优化（G9–G11） |
| D14 | 审计预算与团队 | 上线前置条件 | ① 1 家含 ZK 经验团队 ② 暂不审计 | **①**（②不可接受） | 【阻断】无审计不得上线 |
| D15 | 多签持有人与阈值 | 决定治理安全 | ① 3/5 ② 2/3 ③ 单人 | **① 3/5**（③不可接受） | 阻断部署脚本与治理配置 |
| D16 | 上线日期与灰度规模 | 决定排期与灰度范围 | 起始规模建议 ≥100 人 | **≥100 人、≥3 种钱包环境** | 非阻断，但影响 T13 的验收口径 |
| D17 | 名册导入格式 + commitment 由谁生成 | 决定前后端接口契约 | ① 后端生成 ② 用户端生成 | **① 后端生成**（明确禁止返回身份明文） | 【阻断】Merkle 树一致性对不上，全员 proof 失效 |

### B 类：需要**核实 / 提供**（无取舍，只需给答案）

| # | 需核实信息 | 为什么必须 |
| --- | --- | --- |
| F1 | 现有实名 / KYC 系统及其接口文档 | 决定 S2 阶段是复用还是新建，工期差异约 2 周 |
| F2 | 名册来源系统与可用字段 | 决定 D17 的实现；字段不足需补录流程 |
| F3 | 是否已有 RPC 账号（Alchemy / Infura 等） | 影响 T02 环境搭建与 T09 前端配置 |
| F4 | IPFS Pinning 服务商 | 影响元数据可用性与合规（D11 联动） |
| F5 | 团队技术栈熟练度（Solidity / ZK） | 决定 D9 的取舍与外部支援需求 |
| F6 | 预算科目与上限 | 覆盖审计 + RPC + Pinning + 可能的代付 gas |
| F7 | 合规 / 法务对接人 | D1、D11 需其确认，否则决策无效 |
| F8 | 运维与客服责任人 | 6.6 应急流程需要具名责任人 |
| F9 | 是否需要移动端 H5 / 小程序入口 | 影响 T09 钱包接入方案（WalletConnect 必要性） |
| F10 | 目标选举的时间点与参与规模 | 反向校验排期是否可行 |

---

## 第二部分 · 行动清单（按依赖顺序）

| # | 事项 | 输入 | 预期产出 | 完成标准 | 依赖确认项 |
| --- | --- | --- | --- | --- | --- |
| T01 | 立项评审，冻结全部待确认项 | 本文档第一部分 | 《需求基线 v1.0》签字版 | 17 项均给出结论或「负责人 + 截止日」 | **是（全部）** |
| T02 | P0 环境搭建 | T01 结论 | 可编译空工程 + CI 骨架 + `.env.example` | `forge build`/`forge test` 零错零警；`anvil` 可启动；前端 `dev` 可访问 | 是（D6、D7、F3） |
| T03 | 实现基础库 `Errors` / `Roles` / `BallotCodec` | T02 | 3 个纯函数库 + 单测 | 行覆盖 ≥ 95%；`forge fmt --check` 通过 | 是（D4） |
| T04 | 实现核心合约（先公开投票版，不接 ZK） | T03 | `VoterRegistry` → `ElectionFactory` → `Election` | FR-01~FR-08 用例全过；行覆盖 ≥ 90% | 是（D2、D3、D4、D5） |
| T05 | 补齐单元 + fuzz 测试 | T04 | 完整 `test/unit`、`test/fuzz` | CI profile 10,000 runs 全过；Slither 无 High/Medium | 否 |
| T06 | 集成匿名凭证 | T05、D9 结论 | Groth16 Verifier 接入 `castVote` | 伪造 proof、错误 root、错误 scope、跨选举重放 **100% revert** | 是（D2、D9） |
| T07 | 不变量测试 INV-1~INV-5 | T06 | `test/invariant` | 512 runs × 64 depth 无破坏 | 否 |
| T08 | 部署脚本 + Timelock + 多签 | T07 | `script/Deploy.s.sol` + `deployments/*.json` | anvil 二次执行地址一致；部署者 EOA **无**业务角色；延时 ≥ 24h；浏览器源码可验证 | 是（D15） |
| T09 | 前端骨架 + 钱包接入 | T02 | wagmi 配置、链切换、连接组件 | ≥ 2 种钱包（如 MetaMask + WalletConnect）可连接并切链 | 是（D7、F9） |
| T10 | 投票流程端到端 | T04、T08、T09 | 登录 → 领 proof → 签名 → 回执 全链路 | 拒签 / gas 不足 / 链错误 / 断网 4 类异常均有明确提示且无脏状态 | 是（D17） |
| T11 | 索引器 + 结果页 + 票据校验 | T10 | Ponder 索引 + 结果页 + 校验入口 | 重放结果与链上 `getResult` 完全一致；10 万票首屏 < 2s | 否 |
| T12 | 审计证据包生成 | T11 | `events.jsonl` / `tally.csv` / `verify.mjs` / `checksums.txt` | **仅凭 RPC 地址**在干净环境可复算出同一 `resultHash` | 否 |
| T13 | 测试网灰度 | T12 | 灰度报告（10 项清单） | ≥ 100 名真实用户、≥ 3 种钱包；投票成功率 100%；cost 偏差 ≤ 20% | 是（D16） |
| T14 | 第三方审计与修复闭环 | T13、D14 | 审计报告 + 修复记录 | Critical / High **全部闭环**，无遗留 | 是（D14） |
| T15 | 上线发布 | T14 | 生产部署 + 公示页 | 6.5 发布 Checklist 全部勾选并签字 | 是（D7、D15） |
| T16 | 上线后监控与复盘 | T15 | 告警配置 + T+7 复盘 | 失败率 / 权限变更 / 索引滞后告警均已触发演练成功 | 否 |

**关键路径**：`T01 → T02 → T03 → T04 → T06 → T08 → T10 → T11 → T12 → T13 → T14 → T15`

**两个最大分支点**：`D2`（匿名强度）与 `D6`（公开链/联盟链）。这两项若在 T04 开工后才变更，返工范围覆盖合约、前端、索引器全部三层。

**可并行**：`T09 前端骨架` 与 `T03–T07 合约开发` 互不阻塞，可分线推进。

---

## 第三部分 · 缺失关键信息与假设

### 3.1 缺失的关键信息（未获取前无法定稿）

| # | 缺失信息 | 阻塞对象 |
| --- | --- | --- |
| M1 | 目标选举的参与规模与时间点 | D5、D13、T01 |
| M2 | 是否已有实名/KYC 系统及接口 | S2 阶段方案、D17 |
| M3 | 团队 Solidity / ZK 实际经验水平 | D9、外部支援预算 |
| M4 | 项目预算上限 | D13、D14、D8 |
| M5 | 合规/法务是否已介入 | D1、D11 |
| M6 | 是否有既定的技术栈或云平台约束 | D6、T02 |

### 3.2 已采用的假设（若假设不成立，需回到对应决策点）

| 编号 | 假设 | 若假设不成立的影响 |
| --- | --- | --- |
| AS-01 | 全新工程，无既有代码与遗留约束 | 需追加代码迁移与兼容层，T02 工期 +1~2 周 |
| AS-02 | 团队具备 EVM/Solidity 基础，无 ZK 经验 | 若完全无 EVM 经验，T03–T07 排期需整体 ×1.5 |
| AS-03 | 单场选民 ≤ 10 万 | 超出则去重方案从「位图」改为「仅 votesRoot 渐进树」，T04 需重做 |
| AS-04 | 投票权不具金融属性，不发行代币 | 违反则触发合规红线，方案作废 |
| AS-05 | 允许使用公开链（无强制联盟链要求） | 若强制联盟链，D6 改为备选方案，**公开可验证性丧失**，需另建「哈希锚定回公开链」补偿 |
| AS-06 | 预算可覆盖 1 次第三方审计 | 若不可，则不具备上线条件，只能停留在测试网 |
| AS-07 | 上线窗口 ≥ 8 周 | 短于 6 周则必须砍掉 ZK 匿名（D2 降级为「仅隐身份」或「实名公开」） |
| AS-08 | 用户以浏览器/移动端浏览器访问，无原生 App | 若需原生 App，T09 需增加 Wallet SDK 与深链适配，工时 +2 周 |

---

## 一句话结论

**先做两件事**：① 召开发起 T01 冻结 D1–D17，其中 **D2 与 D6 必须最先拍板**；② 并行补齐 M1–M6 六项缺失信息。T02 之前的所有动作都不应开工。
